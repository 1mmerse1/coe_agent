# 公众号文章入库工具

这个小工具把公众号文章链接整理到本地文章库。你不用安装数据库软件，也不用写数据库代码。

## 第一次运行

双击根目录里的 **开始导入.bat**。它会读取旁边的 `推送URL_test.txt`，逐条导入其中的文章。首次运行如果电脑缺少所需组件，会自动尝试安装。导入完成后窗口会保持打开，输入 `exit` 并回车即可关闭。

运行期间会显示每篇文章的结果：

- `completed`：正文和图片都导入完成。
- `partial`：文章已保存，但有图片没读到，或文章本身只有图片、没有正文文字；提醒会保留下来。
- `failed`：网页或正文没能读出，可以查看原因后重试。
- `skipped`：之前已成功导入，自动跳过，避免重复文章。

## 去哪里看结果

所有结果都放在根目录的 `data` 文件夹：

- `文章清单.csv`：可用 Excel 打开，包含标题、时间、原文链接、导入状态、正文文本和本地预览路径。
- `本次导入失败链接.csv`：如果有文章未能导入，这里会列出对应链接和原因。
- `articles.sqlite3`：程序内部的本地数据库文件，不需要手工打开或编辑。
- `articles`：每篇文章的 HTML 预览和纯文本副本。双击 `.html` 文件可以查看排版；本地保存成功的图片也会显示。
- `media`：下载下来的图片文件，按内容自动去重。
- `snapshots`：抓取时的原始网页压缩备份，以便未来改进解析后重新整理。

## 查看进度和再次运行

双击 `开始导入.bat` 再运行一次即可。已经完成的链接会被跳过；因图片临时下载失败的文章会重试图片。整篇由图片构成的文章不会反复下载。

也可以在此文件夹空白处打开 PowerShell 后执行：

```powershell
python importer.py list
```

重新生成 Excel 清单：

```powershell
python importer.py export
```

如需导入另一份链接文件，可以把文件拖到 `开始导入.bat` 上，或者运行：

```powershell
python importer.py import "另一份链接.txt"
```

链接文件支持每行一条链接，也支持简单 CSV。只处理 `mp.weixin.qq.com` 的文章链接。

## 当前版本的边界

本版本一次处理一篇文章，图片会同时下载；公众号页面如果临时不可访问，会标记失败并可再次运行。文章列表需要先有 URL 清单，工具不会根据公众号名称自动翻出所有历史文章。公众号页面偶尔会调整结构，因此导入结果里会保留原始网页和正文，后续可以修正读取规则后重新整理。

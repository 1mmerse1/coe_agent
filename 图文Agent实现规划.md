# 图文 Agent 实现规划

## 先说结论

可以让推送 Agent 一次生成：

1. 活动推送文案；
2. 首图、正文配图、尾图的图片方案；
3. 每张图的生成提示词；
4. 带图片占位位置的公众号预览页；
5. 通过人工确认后再生成最终图片和导出文件。

第一版不需要重新训练 GPT-6 Sol。当前模型页面显示 GPT-6 Sol 支持图像生成能力，但官方图像接口的实际图片模型是 GPT Image 系列，是否能通过 Aizex 当前地址调用，需要单独检查账号权限和接口兼容性。GPT-6 Sol 本身也不支持传统 fine-tuning，因此更适合先用规则、历史文章和人工确认结果来约束 Agent。

## 适合当前项目的流程

```text
活动说明、时间地点、报名方式、已有照片/Logo
                 ↓
        事实检查 Agent
                 ↓
     检索相似历史推送和配图风格
                 ↓
       文案 Agent + 视觉策划 Agent
                 ↓
   文字初稿 + 图片清单 + 排版结构
                 ↓
         人工确认事实和风格
                 ↓
       图片生成/图片上传/图片替换
                 ↓
       公众号预览页和导出文件
```

## Agent 要接收什么

- 活动名称和一句话说明；
- 活动时间、地点、对象、报名方式；
- 已经确认的嘉宾、流程和联系方式；
- 公众号推送类型；
- 希望的色彩和气氛；
- 学校、学院、学生会 Logo；
- 可使用的活动现场照片；
- 审核人。

没有确认的信息继续使用 `[待确认]`，不能由历史推送推测。

## 一次生成的结果

### 文案部分

沿用当前最高层规则，输出标题、导语、分段正文、活动信息、报名方式、审核署名、排版建议和提交前检查。

### 图片部分

每张图片都生成一条清单，例如：

| 位置 | 用途 | 建议尺寸 | 生成说明 |
|---|---|---:|---|
| 首图 | 文章封面 | 900×383 | 活动主题、校园氛围、留出标题区域 |
| 正文图 1 | 介绍活动亮点 | 900×600 | 展示参与方式，不放未经确认的人名和时间 |
| 正文图 2 | 流程说明 | 900×600 | 用图形和留白表达流程 |
| 尾图 | 结束和提醒 | 900×383 | 呼应主题，保留审核和报名提醒位置 |

图片提示词必须明确：画面内容、构图、色彩、留白位置、比例、是否需要透明背景，以及“不要生成可读文字”。活动名称、时间和二维码由网页排版层后加，避免图片里的文字出错。

## 和现有数据库怎么配合

现有文章库继续保存历史文章和历史图片。新增一张“图文项目”记录即可，里面保存：

- 本次活动资料；
- 生成的文案版本；
- 图片清单和提示词；
- 每张图片的状态（待生成、已生成、已替换、已确认）；
- 最终预览 HTML；
- 人工修改后的版本。

历史图片先用于参考构图和色彩，不直接拿来当本次活动事实，也不直接把所有图片拿去训练。

## 建议分三步做

### 第一步：先生成“文案 + 图片方案”

这是风险最低、最容易验证的一步。页面会一次显示文章初稿和图片清单，用户可以修改后再继续。

### 第二步：接入实际图片生成

为每张图片增加“生成”按钮。生成失败时仍保留图片提示词，不影响文案和预览。首图、尾图建议先生成，正文配图可以先允许上传已有照片。

### 第三步：导出公众号预览包

生成一个本地预览页面，并把图片和 HTML 放进一个文件夹或压缩包。复制到公众号编辑器时，仍需要人工检查图片、链接、时间和报名信息。

## 什么时候才需要真正训练

当积累了至少几十篇“模型初稿 → 人工修改稿”，并且修改方向比较稳定时，再考虑做专门的训练或偏好数据。训练数据应保存：活动资料、历史参考、模型初稿、人工最终稿、修改原因。

在此之前，规则文件、历史案例检索和人工确认更容易控制事实错误，也更方便随时修改风格。

## 建议的第一版接口

```text
POST /api/generate-graphic-push
```

输入活动资料，返回：

```json
{
  "draft": "推送文字初稿",
  "visual_plan": [
    {"position": "cover", "prompt": "图片生成提示词", "size": "900x383"}
  ],
  "preview_html": "本地预览页地址",
  "sources": ["参考历史文章"]
}
```

第一版可以先返回图片方案和占位图，确认流程顺畅后再打开实际图片生成。这样不会因为图像接口权限或生成失败而影响当前的文字 Agent。

## 当前已经接入的部分

- 网页已经改成活动资料向导：活动时间、地点、主题、面向对象、文章类型（预热/总结）、活动介绍、背景/流程/总结文案、策划案、审核人和预使用图片；
- 后端提供 `POST /api/generate-xiumi-push`；
- Agent 只整理文字和排版，不主动生成图片；
- 用户上传的图片会嵌入导出的 HTML，避免本地路径失效；
- 页面返回秀米预览、HTML 下载和复制 HTML 按钮；
- 当前图片来源是本次上传的图片，后续可以把 `assets` 字段接到学院素材库选择器；
- 旧的图文方案接口仍保留作兼容，但新网页流程不再调用图片生成。

### 秀米 HTML 的排版约束

导出 HTML 使用行内样式，避免依赖外部 CSS；正文保持 16 号字、1.8 行距和 2px 字距。图片必须限制最大宽度和最大高度，首尾图与正文图不能直接按原始像素无限放大。标题、导语、正文小标题、活动信息和参与方式分别使用独立的视觉块，方便导入秀米后继续修改。

排版不使用单一固定模板。生成时先按主题、文章类型和活动介绍检索历史文章，再从相似文章中提取背景色、强调色、正文颜色、段落行距、图片排列和分节数量，Agent 选择一个最适合的历史模板风格。本次生成结果会返回“参考模板原文”，便于人工核对。

# 推送档案助手使用说明

## 首次准备

1. 双击 `配置API密钥.bat`，记事本会打开 `.env` 文件。
2. 在 `OPENAI_API_KEY=` 后面填入 Aizex 提供的 API 密钥并保存。`OPENAI_BASE_URL` 已设置为 Aizex 的 Codex API 地址，不需要改。不要把密钥发到聊天里，也不要放进网页文件。
3. 双击 `启动推送助手.bat`。首次启动会准备所需组件，并在浏览器打开本地聊天页面。

本助手通过 Aizex 的 Codex API 地址连接 GPT-6 Sol，推理强度设为 high。账号需要有该模型的可用权限和额度；如果接口临时拒绝 Sol，程序会尝试 `.env` 中配置的备用模型。

## 日常使用

以后双击 `启动推送助手.bat` 即可。窗口显示服务运行信息，关闭窗口会停止本地网页。

聊天页面只能在本机打开。它会先查 `data/articles.sqlite3`，把相关文章片段交给 `gpt-6-sol`，推理强度设为 `high`，并把命中的公众号文章链接列在回答下方。

## 生成秀米推送

点击“写一篇推送”，按页面向导填写活动时间、地点、主题、面向对象、文章类型（预热/总结）、活动介绍、已有的背景/流程/总结文案、策划案和审核人。可以同时选择准备使用的首图、正文图和尾图。

系统会生成带行内样式的 HTML 预览。点击“复制 HTML 到秀米”，在秀米图文编辑器中选择“导入 HTML 代码”后粘贴，再人工修改和检查。图片由你提供，系统不会自行生成图片；以后可以把图片选择位置接入学院素材库。

## 如果密钥暂时还没准备好

网页仍可打开并显示本地文章数量，但聊天请求会提示需要填写密钥。填好后关闭当前服务窗口，再重新双击启动文件即可。

## 常见问题

- **端口已被占用**：如果提示 8000 端口无法使用，请先关闭已经打开的助手窗口再启动。
- **模型请求报错**：检查 `.env` 里的密钥是否正确，以及 API 账户的使用权限和额度。
- **没有找到相关推送**：换成文章中出现的活动名称或关键词；当前版本按文章文字匹配，纯图片中的文字暂时无法搜索。
- **文章清单新增了文章**：先用 `开始导入.bat` 导入；聊天网页每次查询都会直接读取当前本地库。
